/**
 * Controllers Layer (MVC: Controller)
 * 
 * This folder contains all business logic controllers:
 * - LeadController: Manages lead CRUD and CSV import
 * - WorkflowController: Handles workflow creation and execution
 * - CampaignController: Orchestrates campaign lifecycle
 * - AIController: Manages AI message generation via OpenAI
 * 
 * Controllers receive requests from routes, interact with models,
 * and return responses to views.
 */
