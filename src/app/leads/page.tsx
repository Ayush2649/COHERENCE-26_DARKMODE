/**
 * Leads upload page - placeholder until Step 3
 * MVC: View
 */
export default function LeadsPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-8">
      <p className="text-muted-foreground">Lead upload (Step 3) — coming next.</p>
    </main>
  );
}
